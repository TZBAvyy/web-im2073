db.url=jdbc:mysql://{ip_address}:{mysql_port}/{database_name}?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
db.user={database_username}
db.password={database_password}

# Fill in the details in {here}